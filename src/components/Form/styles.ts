import styled from 'styled-components';

export const Container = styled.div`
  position: relative;
  color: #101010;

  .form {
    display: flex;
    flex-direction: column;
    min-width: 50vw;
  }
`;
